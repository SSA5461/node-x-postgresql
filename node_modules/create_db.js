const client = require("./db_config");

client.connect()
    .then(() => client.query("create database db2"))
    .then(() => console.log("database created"))
    .catch(e => console.log("ERROR"))
    .finally(() => client.end());